{
  "name": "obolus-api-basic-examples",
  "private": true,
  "version": "0.1.0",
  "description": "Minimal public examples for the Obolus REST API and remote MCP server.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview",
    "mcp:check": "node scripts/check-mcp.mjs"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
